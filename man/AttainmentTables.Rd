% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{AttainmentTables}
\alias{AttainmentTables}
\title{Age of attainment: log-normal transition parameters}
\format{
A data frame with 162 rows and 7 variables:
\describe{
\item{Sex}{character of sex: "F" or "M"}
\item{Tooth}{character ID of tooth: "Canine", "P3", "P4", "M1", "M2",
or "M3"}
\item{Stage}{character for tooth stage}
\item{log_mu}{numeric mean log age at entering the stage}
\item{log_sd}{numeric log-scale standard deviation, common to all
stages within a sex and tooth}
\item{se_log_mu}{numeric standard error of \code{log_mu}}
\item{n}{integer number of individuals contributing to the transition}
}
}
\source{
Šešelj M, Sherwood RJ, Konigsberg LW. 2019. Timing of Development
of the Permanent Mandibular Dentition: New Reference Values from the Fels
Longitudinal Study. Anat Rec 302:1733-1753. Tables 2-7, panel (a).
}
\usage{
AttainmentTables
}
\description{
Sex-specific ages of attainment from Tables 2-7 of Šešelj et al. (2019),
estimated by transition analysis. Each row describes the distribution of
the age at which a tooth \emph{enters} a stage.
}
\details{
This table is a strict superset of \link{AgeTables}: the same sex, tooth, and
stage combinations, plus twelve \code{Ac} (apex closure) rows that
\code{AgeTables} cannot represent. It is therefore the source for anything
involving the terminal stage.

\code{log_mu} here is \strong{not} comparable to \code{log_mu} in \link{AgeTables}. This is
the age at \emph{entering} a stage; that is the age \emph{given} the stage.

\code{se_log_mu} varies by more than an order of magnitude across rows and
should not be ignored. The two M3 \code{Ac} transitions rest on n = 1 (female)
and n = 5 (male) individuals and have \code{se_log_mu} of 0.0638 and 0.0482,
against 0.011-0.029 for the well-estimated transitions.

\code{Stage} uses the full 14-value vocabulary, but not every stage exists for
every tooth: the single-rooted teeth (Canine, P3, P4) have no \code{Cl.i}
(root cleft initiation) row. Check tooth and stage together, not stage
alone.
}
\seealso{
\link{AgeTables}, \link{StageTies}
}
\keyword{datasets}
