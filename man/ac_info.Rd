% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.dental_age.R
\name{ac_info}
\alias{ac_info}
\title{Terminal-stage information from a dental age estimate}
\usage{
ac_info(x)
}
\arguments{
\item{x}{a \code{dental_age} object.}
}
\value{
A list with the threshold, the binding tooth, the convention used
(\code{q} and \code{method}), the full per-tooth table, the \code{low_precision} and
\code{tied_transition} flags, the tooth names in each category, the interval
level and type, the compatibility code, and the rendered report.
}
\description{
Accessor for the completion-threshold detail attached to the result of
\code{\link[=estimate_dental_age]{estimate_dental_age()}}.
}
\details{
The information travels as an attribute, so it does not survive
arithmetic, \code{c()}, or row-wise \code{sapply()} over several individuals. Those
operations keep the numbers and drop the metadata. Call \code{ac_info()} on
each result before combining.
}
\examples{
x <- data.frame(Sex = "F", Canine = "Ac", P3 = "Ac", P4 = "Ac",
                M1 = "Ac", M2 = "Ac", M3 = NA)
est <- estimate_dental_age(x, verbose = FALSE)
ac_info(est)$threshold
ac_info(est)$binding_tooth

}
\seealso{
\code{\link[=estimate_dental_age]{estimate_dental_age()}}, \code{\link[=ac_completion_threshold]{ac_completion_threshold()}}
}
